# -*- mode: ruby -*-
# vi: set ft=ruby :
# Auto-generated by Vagrantp - DO NOT EDIT

Vagrant.configure("2") do |config|
  config.vm.box = "<%= box %>"

  config.vm.provider "<%= provider %>" do |vb|
    vb.memory = <%= memory %>
    vb.cpus = <%= cpus %>
    vb.disk_size.size = "<%= disk_size %>"
  end

  <% if network_mode == 'bridge' %>
  config.vm.network "public_network", bridge: "<%= bridge_interface %>"
  <% else %>
  config.vm.network "private_network", type: "dhcp"
  <% end %>

  <% if ip_address %>
  config.vm.network "public_network", ip: "<%= ip_address %>"
  <% end %>

  <% if ports %>
  <% ports.each do |port| %>
  <% if port[:auto] %>
  config.vm.network "forwarded_port", guest: <%= port[:container] %>, host: 0, auto_correct: true
  <% else %>
  config.vm.network "forwarded_port", guest: <%= port[:container] %>, host: <%= port[:host] %>
  <% end %>
  <% end %>
  <% end %>

  config.ssh.forward_agent = true
end
